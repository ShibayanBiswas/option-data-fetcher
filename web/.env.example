# Local SQLite (default if unset): file:./data/option_chain.db
# SQLITE_URL=file:./data/option_chain.db

# Vercel / production — Turso (libSQL). Create a DB at https://turso.tech
# then set these on the Vercel project. Same schema as local SQLite.
LIBSQL_URL=libsql://YOUR-DB.turso.io
LIBSQL_AUTH_TOKEN=

# Optional aliases
# TURSO_DATABASE_URL=
# TURSO_AUTH_TOKEN=

CRON_SECRET=change-me
SYNC_SECRET=change-me
